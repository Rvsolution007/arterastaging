# AI System Rules

**CRITICAL RULE FOR ALL AI ASSISTANTS:**
Whenever working in this project directory, you must abide by the following rule regarding "Custom Templates" and their `.json` files:

1. **NO MODIFICATIONS:** You are absolutely prohibited from using your file-editing tools or scripts to modify, write, or overwrite any `.json` files associated with custom template zips (e.g., files inside `skins/` folders, or custom template JSONs).
2. **SUGGESTIONS ONLY:** If a user asks you to fix a template, or if you detect that a template JSON is corrupted or misconfigured, you must ONLY read the JSON file, analyze it, and output a **suggestion** to the user. You must tell the user exactly what lines they need to manually edit. Do NOT run a command or tool to fix the JSON.

This rule is permanent and overrides any implicit desire to fix the files automatically.

const fabric = require('fabric').fabric; const canvas = new fabric.StaticCanvas(null, { width: 200, height: 200 }); const imgData = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAwMCAO+ip1sAAAAASUVORK5CYII='; fabric.Image.fromURL(imgData, function(img) { img.filters = [new fabric.Image.filters.BlendColor({color: '#FF0000', mode: 'tint', alpha: 1})]; img.applyFilters(); canvas.add(img); canvas.renderAll(); console.log('After filter:', img.toDataURL()); img.filters = []; img.applyFilters(); console.log('After clear:', img.toDataURL()); });
